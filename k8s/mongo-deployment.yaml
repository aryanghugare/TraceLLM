apiVersion: apps/v1
kind: Deployment
metadata:
  name: trace-llm-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trace-llm-mongo
  template:
    metadata:
      labels:
        app: trace-llm-mongo
    spec:
      containers:
        - name: mongo
          image: mongo:7
          ports:
            - containerPort: 27017
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: mongo-data
              mountPath: /data/db
      volumes:
        - name: mongo-data
          persistentVolumeClaim:
            claimName: trace-llm-mongo-pvc
