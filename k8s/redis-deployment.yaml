apiVersion: apps/v1
kind: Deployment
metadata:
  name: trace-llm-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trace-llm-redis
  template:
    metadata:
      labels:
        app: trace-llm-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
